Хотим пойти дальше и научить наш `LinkdeList` складываться со встроенным `list`.

1. Добавить в метод `__add__` возможность сложения `LinkdeList` с `list`.
2. Определить метод `__radd__` для сложения `list` с `LinkdeList`.
